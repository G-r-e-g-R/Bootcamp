server:
  port: 8083
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9099/eureka/
Feign:
  hystrix:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "*"